#include<iostream>
#include<iomanip> 
#include <string.h>
#include <vector>
#include <bits/stdc++.h>

int menu();

int main(){
	int opc = 0;
		
	do{
		switch(opc=menu()){
			case 1:
				break;
		}
	}while(opc!=);
	return 0;
}
